Most styles are still in page. 
These override the current styles within this folder.
For consistency these could be externalized and added here,
but may not be needed if single (all-in-one) web application design is used.
